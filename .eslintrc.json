{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-unused-vars": "warn",       // Change to "warn" instead of causing a build failure
    "react/no-unescaped-entities": "error",             // Keep as error, or change to "warn"
    "@next/next/no-img-element": "warn"                 // Change to "warn" instead of error
  }
}
